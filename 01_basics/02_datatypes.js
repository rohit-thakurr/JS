"use strict" // To treat all codes in this file as newer version of JS (now it's by default)

// alert(3+3). // We are using it in nodejs & it can be use in developer console in broweser

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;   // undefined
let newState = null // value is not defined that is it have empty value

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => value is not defined
// symbol =>  use for uniqueness

// object

console.log(typeof age)
console.log( typeof null)   // type is object
console.log( typeof undefined);  // type is undefined